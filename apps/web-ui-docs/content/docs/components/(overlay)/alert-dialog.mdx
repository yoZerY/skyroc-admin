---
title: AlertDialog
description: 需要用户明确确认的重要操作对话框
---

确认对话框（AlertDialog）用于在执行删除、提交、覆盖等关键操作前，要求用户做出明确决策。

```tsx
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogOverlay,
  AlertDialogTitle
} from '@skyroc/web-ui';
```

## 何时使用

- 操作具有风险或不可逆，需要用户再次确认时（如删除数据、重置配置）。
- 流程需要被临时阻塞，必须等待用户确认或取消后才能继续。
- 需要用明确的标题、描述和操作按钮说明决策后果时。
- 普通页面提示使用 Alert；通用内容弹窗使用 Dialog；需要确认关键操作时使用 AlertDialog。

## 语义类型

通过 `type` 设置确认对话框的语义类型。组件会根据类型显示对应的默认图标，帮助用户快速识别操作性质。

| 类型          | 语义     | 适用场景                         |
| ------------- | -------- | -------------------------------- |
| `destructive` | 危险操作 | 删除、清空、覆盖等不可逆操作     |
| `success`     | 成功确认 | 完成、提交、发布等积极结果       |
| `warning`     | 警告确认 | 有潜在风险但仍可继续的操作       |
| `info`        | 信息确认 | 阅读说明、确认已知信息或继续流程 |

<Demo src="@playground/alert-dialog/modules/AlertDialogDestructive" />

<Demo src="@playground/alert-dialog/modules/AlertDialogSuccess" />

<Demo src="@playground/alert-dialog/modules/AlertDialogWarning" />

<Demo src="@playground/alert-dialog/modules/AlertDialogInformation" />

## 尺寸

通过 `size` 调整对话框的宽度、间距、字号和按钮区域间距，从 `xs` 到 `2xl` 共 6 个尺寸。

| 尺寸  | 最大宽度    | 字号 | 适用场景             |
| ----- | ----------- | ---- | -------------------- |
| `xs`  | `max-w-md`  | 10px | 紧凑确认、短文本提示 |
| `sm`  | `max-w-md`  | 12px | 次级操作确认         |
| `md`  | `max-w-lg`  | 14px | 常规场景（默认）     |
| `lg`  | `max-w-xl`  | 16px | 重要操作确认         |
| `xl`  | `max-w-2xl` | 18px | 内容较多的确认流程   |
| `2xl` | `max-w-3xl` | 20px | 大幅内容或重点确认   |

<Demo src="@playground/alert-dialog/modules/AlertDialogSize" />

## 自定义图标

通过 `icon` 传入自定义图标或视觉元素。传入 `icon` 后会覆盖 `type` 对应的默认图标。

<Demo src="@playground/alert-dialog/modules/AlertDialogCustomIcon" />

## 按钮与页脚

默认页脚包含取消按钮和确认按钮。通过 `showCancel={false}` 可以隐藏取消按钮；通过 `okText` 和 `cancelText` 修改按钮文本；通过 `onOk` 和 `onCancel` 监听用户操作。

如果需要完全自定义页脚内容，可通过 `footer` 传入 React 节点；设置为 `null` 或 `false` 时隐藏页脚。

## 自定义样式

通过 `classNames` 精细控制各个 slot 的 class，包括 `overlay`、`content`、`header`、`title`、`description`、`footer` 和 `icon`。

也可以通过 `contentProps`、`headerProps`、`titleProps`、`descriptionProps`、`footerProps`、`overlayProps` 分别向对应结构组件传入额外属性。

## API

### AlertDialog

通用属性参考：基于 Radix UI AlertDialog，支持底层根组件、Portal、Overlay 和 Content 的相关属性。

<PropsTable
  data={[
    {
      name: 'trigger',
      description: '触发对话框打开的元素，通常是 Button',
      type: 'ReactNode'
    },
    {
      name: 'title',
      description: '对话框标题，用于说明当前需要确认的操作',
      type: 'ReactNode'
    },
    {
      name: 'description',
      description: '对话框描述文本，用于补充说明操作影响或风险',
      type: 'string',
      required: true
    },
    {
      name: 'type',
      description: '语义类型，决定默认图标和语义色',
      type: "'destructive' | 'info' | 'success' | 'warning'"
    },
    {
      name: 'size',
      description: '尺寸，影响对话框宽度、字号与间距',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'icon',
      description: '自定义图标，传入后覆盖 type 的默认图标',
      type: 'ReactNode'
    },
    {
      name: 'children',
      description: '插入到描述区域和页脚之间的自定义内容',
      type: 'ReactNode'
    },
    {
      name: 'footer',
      description: '自定义页脚内容；设为 null 或 false 时隐藏页脚',
      type: 'ReactNode | null | false'
    },
    {
      name: 'showCancel',
      description: '是否显示默认取消按钮',
      type: 'boolean',
      default: 'true'
    },
    {
      name: 'okText',
      description: '默认确认按钮文本',
      type: 'ReactNode',
      default: "'OK'"
    },
    {
      name: 'cancelText',
      description: '默认取消按钮文本',
      type: 'ReactNode',
      default: "'Cancel'"
    },
    {
      name: 'onOk',
      description: '点击默认确认按钮时触发',
      type: '(event: React.MouseEvent<HTMLButtonElement>) => void'
    },
    {
      name: 'onCancel',
      description: '点击默认取消按钮时触发',
      type: '(event: React.MouseEvent<HTMLButtonElement>) => void'
    },
    {
      name: 'okButtonProps',
      description: '传递给默认确认按钮的 Button 属性',
      type: 'ButtonProps'
    },
    {
      name: 'cancelButtonProps',
      description: '传递给默认取消按钮的 Button 属性',
      type: 'ButtonProps'
    },
    {
      name: 'classNames',
      description: '各 slot 的自定义 class（overlay / content / header / title / description / footer / icon）',
      type: 'AlertDialogClassNames'
    },
    {
      name: 'contentProps',
      description: '传递给 AlertDialogContent 的额外 props',
      type: 'AlertDialogContentProps'
    },
    {
      name: 'headerProps',
      description: '传递给 AlertDialogHeader 的额外 props',
      type: 'AlertDialogHeaderProps'
    },
    {
      name: 'titleProps',
      description: '传递给 AlertDialogTitle 的额外 props',
      type: 'AlertDialogTitleProps'
    },
    {
      name: 'descriptionProps',
      description: '传递给 AlertDialogDescription 的额外 props',
      type: 'AlertDialogDescriptionProps'
    },
    {
      name: 'footerProps',
      description: '传递给 AlertDialogFooter 的额外 props',
      type: 'AlertDialogFooterProps'
    },
    {
      name: 'overlayProps',
      description: '传递给 AlertDialogOverlay 的额外 props',
      type: 'AlertDialogOverlayProps'
    },
    {
      name: 'forceMountPortal',
      description: '强制挂载 Portal',
      type: 'true'
    },
    {
      name: 'forceMountOverlay',
      description: '强制挂载 Overlay',
      type: 'true'
    },
    {
      name: 'open',
      description: '受控模式下的打开状态',
      type: 'boolean'
    },
    {
      name: 'defaultOpen',
      description: '非受控模式下的默认打开状态',
      type: 'boolean'
    },
    {
      name: 'onOpenChange',
      description: '打开状态变化时触发',
      type: '(open: boolean) => void'
    },
    {
      name: 'className',
      description: '传递给内容容器的自定义 class',
      type: 'ClassValue'
    }
  ]}
/>

### 子组件

AlertDialog 也导出基础结构组件，适合在需要更细粒度组合时使用。

<PropsTable
  data={[
    {
      name: 'AlertDialogAction',
      description: '确认操作按钮，继承 ButtonProps',
      type: 'ButtonProps'
    },
    {
      name: 'AlertDialogCancel',
      description: '取消操作按钮，继承 ButtonProps，默认 variant 为 plain',
      type: 'ButtonProps'
    },
    {
      name: 'AlertDialogContent',
      description: '对话框内容容器，支持对应 Radix Content props、size 和 className',
      type: 'AlertDialogContentProps'
    },
    {
      name: 'AlertDialogDescription',
      description: '描述文本区域，支持对应 Radix Description props、size 和 className',
      type: 'AlertDialogDescriptionProps'
    },
    {
      name: 'AlertDialogFooter',
      description: '页脚容器，支持 div 元素 props、size 和 className',
      type: 'AlertDialogFooterProps'
    },
    {
      name: 'AlertDialogHeader',
      description: '头部容器，支持 div 元素 props、size 和 className',
      type: 'AlertDialogHeaderProps'
    },
    {
      name: 'AlertDialogOverlay',
      description: '遮罩层，支持对应 Radix Overlay props、size 和 className',
      type: 'AlertDialogOverlayProps'
    },
    {
      name: 'AlertDialogTitle',
      description: '标题区域，支持对应 Radix Title props、size 和 className',
      type: 'AlertDialogTitleProps'
    }
  ]}
/>

## 类型

<TypeTable
  data={[
    {
      name: 'AlertDialogProps',
      description:
        '主组件属性。组合了 Radix AlertDialog Root、Content、Portal 的属性，并额外提供标题、描述、按钮、图标和 slot 样式配置。',
      fields: [
        {
          name: 'cancelButtonProps',
          type: 'ButtonProps',
          description: '取消按钮属性。这里只引用 ButtonProps，不展开 Button 的字段。'
        },
        { name: 'cancelText', type: 'React.ReactNode', description: '取消按钮文本。默认值为 Cancel。' },
        {
          name: 'children',
          type: 'React.ReactNode',
          description: '插入到描述区域和页脚之间的内容，也可来自底层 Radix 组件属性。'
        },
        { name: 'className', type: 'ClassValue', description: '内容容器的 class。来自 StyledComponentProps。' },
        { name: 'classNames', type: 'AlertDialogClassNames', description: '各 slot 的自定义 class。' },
        {
          name: 'container',
          type: 'Element | DocumentFragment | null',
          description: 'Portal 挂载容器。来自 Radix Portal props。'
        },
        {
          name: 'contentProps',
          type: 'AlertDialogContentProps',
          description: '传递给 AlertDialogContent 的额外属性。'
        },
        { name: 'defaultOpen', type: 'boolean', description: '非受控模式下的默认打开状态。来自 Radix Root props。' },
        { name: 'description', type: 'string', required: true, description: '对话框描述文本。' },
        {
          name: 'descriptionProps',
          type: 'AlertDialogDescriptionProps',
          description: '传递给 AlertDialogDescription 的额外属性。'
        },
        {
          name: 'disabledPortal',
          type: 'boolean',
          description: '类型中存在，但当前组件实现没有消费该字段；不建议依赖。'
        },
        {
          name: 'footer',
          type: 'React.ReactNode | null | false',
          description: '页脚内容。设为 null 或 false 时隐藏页脚。'
        },
        { name: 'footerProps', type: 'AlertDialogFooterProps', description: '传递给 AlertDialogFooter 的额外属性。' },
        { name: 'forceMount', type: 'true', description: '强制挂载底层 Content 或 Portal。来自 Radix props。' },
        { name: 'forceMountOverlay', type: 'true', description: '强制挂载遮罩层。' },
        { name: 'forceMountPortal', type: 'true', description: '强制挂载 Portal。' },
        { name: 'headerProps', type: 'AlertDialogHeaderProps', description: '传递给 AlertDialogHeader 的额外属性。' },
        { name: 'icon', type: 'React.ReactNode', description: '标题前的图标或视觉元素。' },
        {
          name: 'okButtonProps',
          type: 'ButtonProps',
          description: '确认按钮属性。这里只引用 ButtonProps，不展开 Button 的字段。'
        },
        { name: 'okText', type: 'React.ReactNode', description: '确认按钮文本。默认值为 OK。' },
        {
          name: 'onCancel',
          type: '(e: React.MouseEvent<HTMLButtonElement>) => void',
          description: '点击取消按钮时触发。'
        },
        {
          name: 'onEscapeKeyDown',
          type: '(event: KeyboardEvent) => void',
          description: '按 Escape 触发关闭前的事件。来自 Radix Content props。'
        },
        { name: 'onOk', type: '(e: React.MouseEvent<HTMLButtonElement>) => void', description: '点击确认按钮时触发。' },
        {
          name: 'onOpenAutoFocus',
          type: '(event: Event) => void',
          description: '打开后自动聚焦前触发。来自 Radix Content props。'
        },
        {
          name: 'onOpenChange',
          type: '(open: boolean) => void',
          description: '打开状态变化时触发。来自 Radix Root props。'
        },
        { name: 'open', type: 'boolean', description: '受控模式下的打开状态。来自 Radix Root props。' },
        {
          name: 'overlayProps',
          type: 'AlertDialogOverlayProps',
          description: '传递给 AlertDialogOverlay 的额外属性。'
        },
        { name: 'showCancel', type: 'boolean', description: '是否显示默认取消按钮。默认值为 true。' },
        {
          name: 'size',
          type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
          description: '组件尺寸。来自 StyledComponentProps。'
        },
        { name: 'title', type: 'React.ReactNode', description: '对话框标题。' },
        { name: 'titleProps', type: 'AlertDialogTitleProps', description: '传递给 AlertDialogTitle 的额外属性。' },
        { name: 'trigger', type: 'React.ReactNode', description: '触发对话框打开的元素。' },
        {
          name: 'type',
          type: "'destructive' | 'info' | 'success' | 'warning'",
          description: '语义类型，用于决定默认图标。'
        }
      ]
    },
    {
      name: 'AlertDialogClassNames',
      description: '各 slot 的自定义 class，用于精细化控制 AlertDialog 各部分的样式。',
      fields: [
        { name: 'overlay', type: 'ClassValue', description: '遮罩层的 class。' },
        { name: 'content', type: 'ClassValue', description: '内容容器的 class。' },
        { name: 'header', type: 'ClassValue', description: '头部区域的 class。' },
        { name: 'title', type: 'ClassValue', description: '标题区域的 class。' },
        { name: 'description', type: 'ClassValue', description: '描述文本的 class。' },
        { name: 'footer', type: 'ClassValue', description: '页脚区域的 class。' },
        { name: 'icon', type: 'string', description: '图标元素的 class。' }
      ]
    },
    {
      name: 'AlertDialogContentProps',
      description:
        '内容容器属性。继承 Radix AlertDialog Content props，并通过 StyledComponentProps 增加 className 和 size。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '内容区域子节点。' },
        { name: 'className', type: 'ClassValue', description: '内容容器的 class。' },
        { name: 'forceMount', type: 'true', description: '强制挂载内容容器。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 div 元素属性。' },
        { name: 'onCloseAutoFocus', type: '(event: Event) => void', description: '关闭后自动聚焦前触发。' },
        { name: 'onEscapeKeyDown', type: '(event: KeyboardEvent) => void', description: '按 Escape 时触发。' },
        { name: 'onOpenAutoFocus', type: '(event: Event) => void', description: '打开后自动聚焦前触发。' },
        {
          name: 'size',
          type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
          description: '尺寸，影响内容容器字号、宽度和间距。'
        }
      ]
    },
    {
      name: 'AlertDialogDescriptionProps',
      description:
        '描述文本属性。继承 Radix AlertDialog Description props，并通过 StyledComponentProps 增加 className 和 size。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '描述文本或自定义描述内容。' },
        { name: 'className', type: 'ClassValue', description: '描述区域的 class。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 p 元素属性。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响描述文本字号。' }
      ]
    },
    {
      name: 'AlertDialogFooterProps',
      description: '页脚容器属性。等价于 HTMLComponentProps<\"div\">。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '页脚内容，通常是操作按钮。' },
        { name: 'className', type: 'ClassValue', description: '页脚容器的 class。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 div 元素属性。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响页脚按钮间距。' }
      ]
    },
    {
      name: 'AlertDialogHeaderProps',
      description: '头部容器属性。等价于 HTMLComponentProps<\"div\">。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '头部内容，通常包含标题和描述。' },
        { name: 'className', type: 'ClassValue', description: '头部容器的 class。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 div 元素属性。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响头部内容间距。' }
      ]
    },
    {
      name: 'AlertDialogOverlayProps',
      description:
        '遮罩层属性。继承 Radix AlertDialog Overlay props，并通过 StyledComponentProps 增加 className 和 size。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '遮罩层子节点。通常不需要传入。' },
        { name: 'className', type: 'ClassValue', description: '遮罩层的 class。' },
        { name: 'forceMount', type: 'true', description: '强制挂载遮罩层。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 div 元素属性。' },
        {
          name: 'size',
          type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
          description: '尺寸属性会被接收，但当前遮罩层样式不依赖 size。'
        }
      ]
    },
    {
      name: 'AlertDialogTitleProps',
      description:
        '标题区域属性。继承 Radix AlertDialog Title props，并通过 StyledComponentProps 增加 className 和 size。',
      fields: [
        { name: 'children', type: 'React.ReactNode', description: '标题内容。' },
        { name: 'className', type: 'ClassValue', description: '标题区域的 class。' },
        { name: 'id', type: 'string', description: 'DOM id。来自 h2 元素属性。' },
        {
          name: 'size',
          type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
          description: '尺寸，影响标题字号和图标间距。'
        }
      ]
    },
    {
      name: 'StyledComponentProps<T>',
      description: '项目内用于包装 Radix props 的通用类型。',
      fields: [
        { name: '...T', type: 'Omit<T, \"className\">', description: '继承原始组件属性，但重新定义 className。' },
        { name: 'className', type: 'ClassValue', description: '组件 class。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '项目统一尺寸类型。' }
      ]
    },
    {
      name: 'HTMLComponentProps<T>',
      description: 'HTML 元素属性包装类型，用于 Header 和 Footer 这类 div 容器。',
      fields: [
        {
          name: '...React.ComponentPropsWithRef<T>',
          type: 'React.ComponentPropsWithRef<T>',
          description: '继承指定 HTML 元素的原生属性和 ref。'
        },
        { name: 'className', type: 'ClassValue', description: '组件 class。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '项目统一尺寸类型。' }
      ]
    }
  ]}
/>

<UnionType
  name="DialogSlots"
  description="AlertDialog 可配置 classNames 的内置 slot"
  type="'closeIcon' | 'content' | 'description' | 'footer' | 'header' | 'overlay' | 'title'"
/>

<UnionType
  name="ClassValue"
  description="CSS 类名类型"
  type="string | null | undefined | Record<string, boolean> | ClassValue[]"
/>
