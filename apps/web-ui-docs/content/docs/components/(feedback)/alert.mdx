---
title: Alert
description: 用于向用户传达重要信息或反馈的提示组件
---

提示（Alert）用于在页面中展示简短的提示信息，向用户传达操作结果、系统状态或需要关注的内容。

```tsx
import { Alert, AlertRoot, AlertTitle, AlertDescription, AlertWrapper } from '@skyroc/web-ui';
```

## 何时使用

- 需要向用户反馈操作结果时（如成功、失败、警告）。
- 展示系统级消息或状态变化（如网络错误、版本更新）。
- 在表单或流程中给予用户必要的提示信息。
- 相比 Toast，Alert 更适合需要持续展示在页面中、不会自动消失的场景。

## 颜色

Alert 提供 8 种主题颜色，每种颜色都有其语义含义：

| 颜色          | 语义     | 适用场景                 |
| ------------- | -------- | ------------------------ |
| `primary`     | 主要提示 | 常规信息、引导操作       |
| `destructive` | 危险警告 | 错误信息、不可逆操作警告 |
| `success`     | 成功提示 | 操作成功、流程完成       |
| `warning`     | 警告提示 | 需要注意的潜在问题       |
| `info`        | 信息提示 | 中性信息、辅助说明       |
| `carbon`      | 中性深色 | 不带语义倾向的通用提示   |
| `secondary`   | 次级提示 | 低优先级信息、辅助性提示 |
| `accent`      | 强调提示 | 需要特殊视觉关注的信息   |

<Demo src="@playground/alert/modules/AlertColor" />

## 变体

通过 `variant` 控制 Alert 的视觉强度，从高到低依次为：

| 变体      | 视觉强度 | 适用场景                      |
| --------- | -------- | ----------------------------- |
| `solid`   | 最强     | 紧急信息，填充背景色          |
| `pure`    | 较强     | 中性卡片风格，标题 + 描述场景 |
| `outline` | 中等     | 常规提示，实线边框            |
| `soft`    | 低       | 轻量提示，淡色背景无边框      |
| `ghost`   | 最低     | 带边框的淡色背景（默认）      |

<Demo src="@playground/alert/modules/AlertVariant" />

## 描述文本

通过 `title` 和 `description` 组合展示标题与补充说明，适合需要更多上下文的提示场景。

<Demo src="@playground/alert/modules/AlertDescription" />

## 图标

通过 `icon` 插槽在 Alert 起始位置放置图标，增强信息的可识别性。图标会自动继承当前颜色主题。

<Demo src="@playground/alert/modules/AlertIcon" />

## 完整示例

结合 `icon`、`title`、`description` 和 `trailing` 插槽，构建功能完整的提示组件。`trailing` 可放置关闭按钮或其他操作。

<Demo src="@playground/alert/modules/AlertComplete" />

## 尺寸

通过 `size` 调整 Alert 的字号与间距，从 `xs` 到 `2xl` 共 6 个尺寸。

| 尺寸  | 字号 | 内间距       | 适用场景           |
| ----- | ---- | ------------ | ------------------ |
| `xs`  | 10px | px-2 py-1.75 | 紧凑嵌套、辅助信息 |
| `sm`  | 12px | px-3 py-2.5  | 侧边栏、次级面板   |
| `md`  | 14px | px-4 py-3    | 常规场景（默认）   |
| `lg`  | 16px | px-5 py-3.5  | 强调内容、重要通知 |
| `xl`  | 18px | px-6 py-4    | 突出信息面板       |
| `2xl` | 20px | px-7 py-4.5  | 首屏公告、大幅展示 |

<Demo src="@playground/alert/modules/AlertSize" />

## API

### Alert

通用属性参考：支持原生 `div` 元素的所有属性。

<PropsTable
  data={[
    {
      name: 'color',
      description: '主题颜色',
      type: "'primary' | 'destructive' | 'success' | 'warning' | 'info' | 'carbon' | 'secondary' | 'accent'",
      default: "'primary'"
    },
    {
      name: 'variant',
      description: '视觉样式',
      type: "'solid' | 'pure' | 'outline' | 'soft' | 'ghost'",
      default: "'ghost'"
    },
    {
      name: 'size',
      description: '尺寸，影响字号与间距',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'title',
      description: '标题文本',
      type: 'ReactNode'
    },
    {
      name: 'description',
      description: '描述文本，显示在标题下方',
      type: 'ReactNode'
    },
    {
      name: 'icon',
      description: '图标，显示在 Alert 起始位置',
      type: 'ReactNode'
    },
    {
      name: 'leading',
      description: '前置插槽，位于图标之前',
      type: 'ReactNode'
    },
    {
      name: 'trailing',
      description: '后置插槽，位于内容区之后',
      type: 'ReactNode'
    },
    {
      name: 'classNames',
      description: '各 slot 的自定义 class（root / wrapper / title / description / icon / close / action）',
      type: 'AlertClassNames'
    },
    {
      name: 'titleProps',
      description: '传递给 AlertTitle 的额外 props',
      type: 'AlertTitleProps'
    },
    {
      name: 'descriptionProps',
      description: '传递给 AlertDescription 的额外 props',
      type: 'AlertDescriptionProps'
    },
    {
      name: 'wrapperProps',
      description: '传递给 AlertWrapper 的额外 props',
      type: 'AlertWrapperProps'
    }
  ]}
/>

## 类型

<TypeTable
  data={[
    {
      name: 'AlertClassNames',
      description: '各 slot 的自定义 class，用于精细化控制 Alert 各部分的样式。',
      fields: [
        { name: 'root', type: 'ClassValue', description: '根容器的 class。' },
        { name: 'wrapper', type: 'ClassValue', description: '内容包裹区的 class。' },
        { name: 'title', type: 'ClassValue', description: '标题的 class。' },
        { name: 'description', type: 'ClassValue', description: '描述文本的 class。' },
        { name: 'icon', type: 'ClassValue', description: '图标的 class。' },
        { name: 'close', type: 'ClassValue', description: '关闭按钮的 class。' },
        { name: 'action', type: 'ClassValue', description: '操作区域的 class。' }
      ]
    },
    {
      name: 'AlertTitleProps',
      description: '标题子组件的属性，继承原生 div 元素的所有属性。',
      fields: [
        { name: 'className', type: 'ClassValue', description: '自定义 class。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响标题字号。' }
      ]
    },
    {
      name: 'AlertDescriptionProps',
      description: '描述子组件的属性，继承原生 div 元素的所有属性。',
      fields: [
        { name: 'className', type: 'ClassValue', description: '自定义 class。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响描述字号。' }
      ]
    },
    {
      name: 'AlertWrapperProps',
      description: '内容包裹区子组件的属性，继承原生 div 元素的所有属性。',
      fields: [
        { name: 'className', type: 'ClassValue', description: '自定义 class。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸，影响内容区间距。' }
      ]
    }
  ]}
/>

<UnionType
  name="ClassValue"
  description="CSS 类名类型"
  type="string | null | undefined | Record<string, boolean> | ClassValue[]"
/>
