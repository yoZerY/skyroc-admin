---
title: Avatar
description: 展示用户头像、图片回退内容和头像组合的组件
---

头像（Avatar）用于展示用户、团队、组织或对象的视觉标识。组件基于 Radix Avatar 封装，内置图片加载失败时的回退内容，并通过 `size` 控制头像尺寸。

```tsx
import { Avatar, AvatarFallback, AvatarImage, AvatarRoot } from '@skyroc/web-ui';
```

## 架构说明

Avatar 由三个结构组件组成：

- `AvatarRoot`：头像根容器，负责尺寸、圆角和裁切。
- `AvatarImage`：头像图片，负责渲染 `src` 和 `alt`。
- `AvatarFallback`：图片加载失败或延迟显示时的回退内容。

`Avatar` 主组件会自动组合这三个结构组件，常规使用时只需要传入 `src`、`alt` 和 `fallback`。

## 何时使用

- 展示用户头像、团队标识、应用图标或对象缩略图。
- 图片可能加载失败，需要展示姓名缩写、图标或占位内容。
- 多个用户需要以头像组形式紧凑展示。
- 需要统一控制头像尺寸、圆形裁切和回退样式。

## 基础用法

传入 `src` 设置头像图片，传入 `alt` 提供可访问文本，传入 `fallback` 作为加载失败时的回退内容。

<Demo src="@playground/avatar/modules/AvatarBasic" />

## 尺寸

通过 `size` 控制头像根容器大小，从 `xs` 到 `2xl` 共 6 个尺寸。

| 尺寸  | 容器尺寸 | 适用场景             |
| ----- | -------- | -------------------- |
| `xs`  | 24px     | 表格、紧凑列表       |
| `sm`  | 32px     | 菜单、次级信息       |
| `md`  | 40px     | 常规场景（默认）     |
| `lg`  | 48px     | 用户卡片、详情区域   |
| `xl`  | 56px     | 个人资料、重点展示   |
| `2xl` | 64px     | 大头像、个人主页头部 |

<Demo src="@playground/avatar/modules/AvatarSize" />

## 回退内容

当图片加载失败或不可用时，`fallback` 会显示在头像容器中。常见内容包括姓名缩写、符号或图标。

<Demo src="@playground/avatar/modules/AvatarFallback" />

## 自定义回退

`fallback` 支持任意 React 节点，可放入图标、自定义图片或其他占位内容。通过 `classNames.fallback` 或 `fallbackProps` 可调整回退区域样式。

<Demo src="@playground/avatar/modules/AvatarCustomFallback" />

## 头像组

多个 Avatar 可以通过布局 class 组合成头像组。需要重叠展示时，可在外层使用负间距，并通过 `className` 或 `classNames.root` 添加描边。

<Demo src="@playground/avatar/modules/AvatarGroup" />

## 自定义样式

通过 `classNames` 精细控制 `root`、`image`、`fallback` 三个 slot 的 class。

也可以通过 `rootProps`、`fallbackProps` 分别向根容器和回退内容传入额外属性；主组件自身继承图片属性，`className` 默认作用在 `AvatarImage` 上。

## API

### Avatar

通用属性参考：基于 Radix UI Avatar，主组件继承 `AvatarImage` 的图片属性，并额外支持回退内容和根容器配置。

<PropsTable
  data={[
    {
      name: 'src',
      description: '头像图片地址',
      type: 'string'
    },
    {
      name: 'alt',
      description: '头像图片替代文本',
      type: 'string'
    },
    {
      name: 'fallback',
      description: '图片加载失败或等待加载时显示的回退内容',
      type: 'ReactNode'
    },
    {
      name: 'size',
      description: '头像尺寸，作用于根容器',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'delayMs',
      description: '延迟显示 fallback 的毫秒数',
      type: 'number'
    },
    {
      name: 'className',
      description: '头像图片的 class',
      type: 'ClassValue'
    },
    {
      name: 'classNames',
      description: '各 slot 的自定义 class（root / image / fallback）',
      type: 'AvatarUi'
    },
    {
      name: 'fallbackProps',
      description: '传递给 AvatarFallback 的额外 props',
      type: 'AvatarFallbackProps'
    },
    {
      name: 'rootProps',
      description: '传递给 AvatarRoot 的额外 props',
      type: 'AvatarRootProps'
    },
    {
      name: 'onLoadingStatusChange',
      description: '图片加载状态变化时触发',
      type: '(status: "idle" | "loading" | "loaded" | "error") => void'
    },
    {
      name: 'ref',
      description: '指向底层 img 元素的 ref',
      type: 'Ref<HTMLImageElement>'
    }
  ]}
/>

### 子组件

Avatar 也导出基础结构组件，适合需要手动组合 Root、Image、Fallback 的场景。

<PropsTable
  data={[
    {
      name: 'AvatarRoot',
      description: '头像根容器，支持 Radix Avatar Root props、size 和 className',
      type: 'AvatarRootProps'
    },
    {
      name: 'AvatarImage',
      description: '头像图片，支持 Radix Avatar Image props、size 和 className',
      type: 'AvatarImageProps'
    },
    {
      name: 'AvatarFallback',
      description: '回退内容，支持 Radix Avatar Fallback props、delayMs、size 和 className',
      type: 'AvatarFallbackProps'
    }
  ]}
/>

## 类型

<TypeTable data={[
  {
    name: 'AvatarProps',
    description: '主组件属性。继承 AvatarImageProps 和 AvatarFallbackProps 中的 delayMs，并额外提供 fallback、classNames、rootProps 和 fallbackProps。',
    fields: [
      { name: 'alt', type: 'string', description: '头像图片替代文本。来自 img 元素属性。' },
      { name: 'className', type: 'ClassValue', description: '图片元素的 class。来自 StyledComponentProps。' },
      { name: 'classNames', type: 'AvatarUi', description: '各 slot 的自定义 class。' },
      { name: 'delayMs', type: 'number', description: '延迟显示 fallback 的毫秒数。来自 AvatarFallbackProps。' },
      { name: 'fallback', type: 'React.ReactNode', description: '图片不可用时展示的回退内容。' },
      { name: 'fallbackProps', type: 'AvatarFallbackProps', description: '传递给 AvatarFallback 的额外属性。' },
      { name: 'onLoadingStatusChange', type: `(status: 'idle' | 'loading' | 'loaded' | 'error') => void`, description: '图片加载状态变化回调。来自 Radix Avatar Image props。' },
      { name: 'ref', type: 'Ref<HTMLImageElement>', description: '转发到底层图片元素的 ref。' },
      { name: 'rootProps', type: 'AvatarRootProps', description: '传递给 AvatarRoot 的额外属性。' },
      { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '头像尺寸。来自 StyledComponentProps。' },
      { name: 'src', type: 'string', description: '头像图片地址。来自 img 元素属性。' },
      { name: '...React.ImgHTMLAttributes<HTMLImageElement>', type: 'React.ImgHTMLAttributes<HTMLImageElement>', description: '支持其余标准 img 属性。' }
    ]
  },
  {
    name: 'AvatarUi',
    description: 'Avatar 各 slot 的自定义 class 配置。',
    fields: [
      { name: 'root', type: 'ClassValue', description: '根容器的 class。' },
      { name: 'image', type: 'ClassValue', description: '图片元素的 class。' },
      { name: 'fallback', type: 'ClassValue', description: '回退内容的 class。' }
    ]
  },
  {
    name: 'AvatarRootProps',
    description: '根容器属性。继承 Radix Avatar Root props，并通过 StyledComponentProps 增加 className 和 size。',
    fields: [
      { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。来自 Radix Primitive。' },
      { name: 'children', type: 'React.ReactNode', description: '根容器子节点，通常是 AvatarImage 和 AvatarFallback。' },
      { name: 'className', type: 'ClassValue', description: '根容器 class。' },
      { name: 'id', type: 'string', description: 'DOM id。来自 span 元素属性。' },
      { name: 'ref', type: 'Ref<HTMLSpanElement>', description: '转发到底层 span 元素的 ref。' },
      { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '头像尺寸，影响根容器大小。' }
    ]
  },
  {
    name: 'AvatarImageProps',
    description: '图片属性。继承 Radix Avatar Image props，并通过 StyledComponentProps 增加 className 和 size。',
    fields: [
      { name: 'alt', type: 'string', description: '图片替代文本。' },
      { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。来自 Radix Primitive。' },
      { name: 'className', type: 'ClassValue', description: '图片 class。' },
      { name: 'height', type: 'number | string', description: '图片高度。来自 img 元素属性。' },
      { name: 'onLoadingStatusChange', type: `(status: 'idle' | 'loading' | 'loaded' | 'error') => void`, description: '图片加载状态变化回调。' },
      { name: 'ref', type: 'Ref<HTMLImageElement>', description: '转发到底层 img 元素的 ref。' },
      { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸属性会被接收；当前图片样式主要依赖根容器尺寸。' },
      { name: 'src', type: 'string', description: '图片地址。' },
      { name: 'width', type: 'number | string', description: '图片宽度。来自 img 元素属性。' },
      { name: '...React.ImgHTMLAttributes<HTMLImageElement>', type: 'React.ImgHTMLAttributes<HTMLImageElement>', description: '支持其余标准 img 属性与事件。' }
    ]
  },
  {
    name: 'AvatarFallbackProps',
    description: '回退内容属性。继承 Radix Avatar Fallback props，并通过 StyledComponentProps 增加 className 和 size。',
    fields: [
      { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。来自 Radix Primitive。' },
      { name: 'children', type: 'React.ReactNode', description: '回退内容。' },
      { name: 'className', type: 'ClassValue', description: '回退内容 class。' },
      { name: 'delayMs', type: 'number', description: '延迟显示 fallback 的毫秒数。' },
      { name: 'id', type: 'string', description: 'DOM id。来自 span 元素属性。' },
      { name: 'ref', type: 'Ref<HTMLSpanElement>', description: '转发到底层 span 元素的 ref。' },
      { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '尺寸属性会被接收；当前回退内容样式主要依赖根容器尺寸。' }
    ]
  },
  {
    name: 'StyledComponentProps<T>',
    description: '项目内用于包装 Radix props 的通用类型。',
    fields: [
      { name: '...T', type: 'Omit<T, \"className\">', description: '继承原始组件属性，但重新定义 className。' },
      { name: 'className', type: 'ClassValue', description: '组件 class。' },
      { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '项目统一尺寸类型。' }
    ]
  },
  {
    name: 'RadixAvatarRootProps',
    description: 'Radix Avatar Root 原始属性。',
    fields: [
      { name: '...PrimitiveSpanProps', type: 'PrimitiveSpanProps', description: '继承 Radix Primitive span 属性。' }
    ]
  },
  {
    name: 'RadixAvatarImageProps',
    description: 'Radix Avatar Image 原始属性。',
    fields: [
      { name: '...PrimitiveImageProps', type: 'PrimitiveImageProps', description: '继承 Radix Primitive img 属性。' },
      { name: 'onLoadingStatusChange', type: `(status: 'idle' | 'loading' | 'loaded' | 'error') => void`, description: '图片加载状态变化回调。' }
    ]
  },
  {
    name: 'RadixAvatarFallbackProps',
    description: 'Radix Avatar Fallback 原始属性。',
    fields: [
      { name: '...PrimitiveSpanProps', type: 'PrimitiveSpanProps', description: '继承 Radix Primitive span 属性。' },
      { name: 'delayMs', type: 'number', description: '延迟显示 fallback 的毫秒数。' }
    ]
  },
  {
    name: 'PrimitiveSpanProps',
    description: 'Radix Primitive span 属性。',
    fields: [
      { name: '...React.ComponentPropsWithoutRef<typeof Primitive.span>', type: 'React.ComponentPropsWithoutRef<typeof Primitive.span>', description: '继承 Primitive.span 的无 ref 属性。' }
    ]
  },
  {
    name: 'PrimitiveImageProps',
    description: 'Radix Primitive img 属性。',
    fields: [
      { name: '...React.ComponentPropsWithoutRef<typeof Primitive.img>', type: 'React.ComponentPropsWithoutRef<typeof Primitive.img>', description: '继承 Primitive.img 的无 ref 属性。' }
    ]
  },
  {
    name: 'PrimitivePropsWithRef<E>',
    description: 'Radix Primitive 通用属性类型。',
    fields: [
      { name: '...React.ComponentPropsWithRef<E>', type: 'React.ComponentPropsWithRef<E>', description: '继承指定元素的原生属性和 ref。' },
      { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。' }
    ]
  }
]} />

<UnionType name="AvatarSlots" description="Avatar 可配置 classNames 的 slot" type="'fallback' | 'image' | 'root'" />

<UnionType
  name="ClassValue"
  description="CSS 类名类型"
  type="string | null | undefined | Record<string, boolean> | ClassValue[]"
/>
