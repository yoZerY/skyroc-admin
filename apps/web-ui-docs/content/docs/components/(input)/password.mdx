---
title: Password
description: 带密码可见性切换能力的输入框组件
---

Password 用于输入密码、密钥等敏感文本。组件基于 Input 封装，保留输入框的尺寸、插槽、清空按钮和原生 input 属性，并额外提供密码明文/密文切换能力。

```tsx
import { Password } from '@skyroc/web-ui';
```

## 何时使用

- 需要输入密码、确认密码、访问令牌或密钥。
- 需要让用户临时查看输入内容，减少密码输入错误。
- 需要复用 Input 的尺寸、插槽、清空按钮和表单属性。
- 普通单行文本输入使用 Input；需要数值步进时使用 NumberInput。

## 基础用法

默认情况下，Password 使用密文输入，并在右侧显示可见性切换按钮。

<Demo src="@playground/password/modules/PasswordBase" />

```tsx
<Password placeholder="Please input password" />
```

## 受控可见性

通过 `visible` 和 `onVisibleChange` 控制密码是否明文显示；非受控模式使用 `defaultVisible`。

<Demo src="@playground/password/modules/PasswordControlled" />

```tsx
const [visible, setVisible] = useState(false);

<Password visible={visible} onVisibleChange={setVisible} />
```

## 可清空

通过 `clearable` 显示清空按钮。清空按钮来自 Input，会清空当前 DOM value 并触发 input 事件。

<Demo src="@playground/password/modules/PasswordClearable" />

```tsx
<Password clearable defaultValue="abc123" />
```

## 自定义图标

通过 `hiddenIcon` 和 `visibleIcon` 替换默认的 `EyeOff` / `Eye` 图标。`hiddenIcon` 在密码处于隐藏状态时显示，`visibleIcon` 在密码处于明文状态时显示。

<Demo src="@playground/password/modules/PasswordIcon" />

```tsx
<Password
  hiddenIcon={<Lock />}
  visibleIcon={<LockOpen />}
/>
```

## 禁用

通过 `disabled` 禁用输入框。Password 继承 Input 的只读逻辑，`readOnly` 也会让底层 input 不可编辑。

<Demo src="@playground/password/modules/PasswordDisabled" />

```tsx
<Password disabled defaultValue="abc123" />
```

## 尺寸

通过 `size` 调整高度、字号、内边距和插槽间距，从 `xs` 到 `2xl` 共 6 个尺寸。

<Demo src="@playground/password/modules/PasswordSize" />

| 尺寸  | 高度 | 字号 | 适用场景           |
| ----- | ---- | ---- | ------------------ |
| `xs`  | 24px | 10px | 紧凑筛选、表格内嵌 |
| `sm`  | 28px | 12px | 辅助表单、侧边栏   |
| `md`  | 32px | 14px | 常规场景（默认）   |
| `lg`  | 36px | 16px | 重点表单项         |
| `xl`  | 40px | 18px | 大号输入区域       |
| `2xl` | 48px | 20px | 大幅表单或首屏输入 |

## API

### Password

通用属性参考：继承 Input 的常用属性和原生 input 属性，但底层 `type` 由可见性状态自动控制。

<PropsTable
  data={[
    {
      name: 'value',
      description: '受控输入值',
      type: 'string | number | readonly string[]'
    },
    {
      name: 'defaultValue',
      description: '非受控模式下的默认值',
      type: 'string | number | readonly string[]'
    },
    {
      name: 'onChange',
      description: '输入值变化时触发',
      type: '(event) => void'
    },
    {
      name: 'placeholder',
      description: '占位文本',
      type: 'string'
    },
    {
      name: 'visible',
      description: '受控模式下密码是否明文显示',
      type: 'boolean'
    },
    {
      name: 'defaultVisible',
      description: '非受控模式下密码是否默认明文显示',
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'onVisibleChange',
      description: '可见性状态变化时触发',
      type: '(visible: boolean) => void'
    },
    {
      name: 'hiddenIcon',
      description: '密码隐藏时显示的切换图标',
      type: 'ReactNode',
      default: '<EyeOff />'
    },
    {
      name: 'visibleIcon',
      description: '密码明文显示时显示的切换图标',
      type: 'ReactNode',
      default: '<Eye />'
    },
    {
      name: 'clearable',
      description: '是否显示清空按钮',
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'disabled',
      description: '是否禁用输入框',
      type: 'boolean'
    },
    {
      name: 'readOnly',
      description: '是否只读；当前实现会同时禁用底层 input 的编辑能力',
      type: 'boolean'
    },
    {
      name: 'leading',
      description: '前置插槽，显示在输入框左侧',
      type: 'ReactNode'
    },
    {
      name: 'trailing',
      description: '后置插槽，显示在清空按钮和可见性按钮之间',
      type: 'ReactNode'
    },
    {
      name: 'size',
      description: '尺寸，影响高度、字号、内边距和插槽间距',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'className',
      description: '自定义 class；继承 Input 当前的兜底 class 行为',
      type: 'ClassValue'
    },
    {
      name: 'classNames',
      description: 'Input 各 slot 的自定义 class（root / control / clearable / visible）',
      type: 'InputClassNames'
    },
    {
      name: 'name',
      description: '表单字段名称',
      type: 'string'
    },
    {
      name: 'required',
      description: '是否必填',
      type: 'boolean'
    },
    {
      name: 'autoComplete',
      description: '浏览器自动完成策略；默认值为 off，可通过 props 覆盖',
      type: 'string',
      default: "'off'"
    },
    {
      name: 'autoFocus',
      description: '是否自动聚焦',
      type: 'boolean'
    }
  ]}
/>

## 类型

<TypeTable
  data={[
    {
      name: 'PasswordProps',
      description: 'Password 组件属性。继承 InputProps，并增加密码可见性控制相关字段。',
      fields: [
        { name: 'value', type: 'string | number | readonly string[]', description: '受控输入值。' },
        { name: 'defaultValue', type: 'string | number | readonly string[]', description: '非受控默认值。' },
        { name: 'onChange', type: '(event) => void', description: '输入值变化回调。' },
        { name: 'placeholder', type: 'string', description: '占位文本。' },
        { name: 'visible', type: 'boolean', description: '受控可见性状态。' },
        { name: 'defaultVisible', type: 'boolean', description: '非受控默认可见性状态。' },
        { name: 'onVisibleChange', type: '(visible: boolean) => void', description: '可见性变化回调。' },
        { name: 'hiddenIcon', type: 'ReactNode', description: '密码隐藏时显示的切换图标。' },
        { name: 'visibleIcon', type: 'ReactNode', description: '密码明文显示时显示的切换图标。' },
        { name: 'clearable', type: 'boolean', description: '是否显示清空按钮。' },
        { name: 'disabled', type: 'boolean', description: '是否禁用。' },
        { name: 'readOnly', type: 'boolean', description: '是否只读。' },
        { name: 'leading', type: 'ReactNode', description: '前置插槽内容。' },
        { name: 'trailing', type: 'ReactNode', description: '后置插槽内容，会显示在可见性按钮之前。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '组件尺寸。' },
        { name: 'className', type: 'ClassValue', description: '自定义 class。' },
        { name: 'classNames', type: 'InputClassNames', description: 'Input slot class 配置。' },
        { name: 'name', type: 'string', description: '表单字段名称。' },
        { name: 'required', type: 'boolean', description: '是否必填。' },
        { name: 'autoComplete', type: 'string', description: '浏览器自动完成策略。' },
        { name: 'autoFocus', type: 'boolean', description: '是否自动聚焦。' },
        { name: '...inputProps', type: 'input element props without native size', description: '其他原生 input 属性；底层 type 由 Password 自动控制。' }
      ]
    },
    {
      name: 'InputClassNames',
      description: 'Password 继承 Input 的 slot class 配置。',
      fields: [
        { name: 'root', type: 'ClassValue', description: '外层容器的 class。' },
        { name: 'control', type: 'ClassValue', description: '底层 input 的 class。' },
        { name: 'clearable', type: 'ClassValue', description: '清空按钮图标的 class。' },
        { name: 'visible', type: 'ClassValue', description: 'Input 变体中预留的可见状态 slot，Password 当前没有直接使用该 slot。' }
      ]
    }
  ]}
/>

<UnionType
  name="ClassValue"
  description="CSS 类名类型"
  type="string | null | undefined | Record<string, boolean> | ClassValue[]"
/>
