/**
 * Native spacing & radius tokens.
 *
 * Sourced from `@skyroc/ui-tokens` (single cross-platform source of truth).
 * Re-exported here so existing imports stay stable.
 */
export { borderRadius, spacing } from '@skyroc/ui-tokens';
