{
  "name": "@skyroc/config",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Shared development configuration for Skyroc Admin (vitest, vite, etc.)",
  "exports": {
    "./vitest": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch"
  },
  "devDependencies": {
    "@skyroc/tsconfig": "workspace:*",
    "vitest": "catalog:dev"
  }
}
