---
title: Switch
description: 用于在开/关两种状态之间切换的开关组件
---

开关（Switch）用于在两种互斥状态之间切换，常见于设置面板中的功能启停。基于 Radix UI Switch 封装，支持主题颜色、多种尺寸和无障碍键盘操作。

```tsx
import {
  Switch,
  SwitchRoot,
  SwitchThumb
} from '@skyroc/web-ui';
```

## 何时使用

- 需要在"开"和"关"两种状态之间切换时。
- 设置面板、偏好配置等需要即时生效的布尔选项。
- 需要清晰表达当前状态，并允许用户立即改变状态。
- 如果需要从一组选项中多选，使用 Checkbox；如果状态需要提交后才生效，优先使用 Checkbox 或表单控件组合。

## 基础用法

通过 `defaultChecked` 设置非受控初始状态。Switch 本身没有可见文本，建议始终提供 `aria-label` 或配合外部文本说明。

<Demo src="@playground/switch/modules/SwitchBasic" />

```tsx
<Switch
  defaultChecked
  aria-label="Enable notifications"
/>
```

## 受控模式

使用 `checked` 和 `onCheckedChange` 管理开关状态。`onCheckedChange` 会返回新的布尔值。

<Demo src="@playground/switch/modules/SwitchControlled" />

```tsx
const [checked, setChecked] = useState(false);

<Switch checked={checked} onCheckedChange={setChecked} aria-label="开启通知" />
```

## 颜色

通过 `color` 控制开关打开时的颜色，支持全部 8 种语义色。

<Demo src="@playground/switch/modules/SwitchColor" />

```tsx
<Switch color="primary" defaultChecked />
<Switch color="success" defaultChecked />
<Switch color="warning" defaultChecked />
<Switch color="info" defaultChecked />
<Switch color="destructive" defaultChecked />
<Switch color="carbon" defaultChecked />
<Switch color="secondary" defaultChecked />
<Switch color="accent" defaultChecked />
```

## 尺寸

通过 `size` 调整开关大小，从 `xs` 到 `2xl` 共 6 个尺寸。

<Demo src="@playground/switch/modules/SwitchSize" />

| 尺寸  | 轨道尺寸   | 滑块尺寸 | 适用场景           |
| ----- | ---------- | -------- | ------------------ |
| `xs`  | 28×16px    | 12px     | 紧凑列表、表格内嵌 |
| `sm`  | 32×18px    | 14px     | 侧边栏设置项       |
| `md`  | 36×20px    | 16px     | 常规场景（默认）   |
| `lg`  | 40×22px    | 18px     | 强调设置项         |
| `xl`  | 44×24px    | 20px     | 大号设置面板       |
| `2xl` | 52×28px    | 24px     | 首屏重要开关       |

```tsx
<Switch size="xs" defaultChecked />
<Switch size="sm" defaultChecked />
<Switch size="md" defaultChecked />
<Switch size="lg" defaultChecked />
<Switch size="xl" defaultChecked />
<Switch size="2xl" defaultChecked />
```

## 禁用

设置 `disabled` 后，开关变为不可交互状态。

<Demo src="@playground/switch/modules/SwitchDisabled" />

```tsx
<Switch disabled />
<Switch disabled defaultChecked />
```

## 自定义样式

通过 `classNames` 分别控制 `root`（轨道）和 `thumb`（滑块）的样式。

<Demo src="@playground/switch/modules/SwitchCustom" />

```tsx
<Switch
  classNames={{
    root: 'data-[state=checked]:bg-gradient-to-r data-[state=checked]:from-pink-500 data-[state=checked]:to-violet-500',
    thumb: 'bg-white'
  }}
/>
```

## API

### Switch

通用属性参考：组合 `SwitchRoot` 和 `SwitchThumb` 的主组件。支持 Radix Switch Root 的常用属性和原生 button 属性。

<PropsTable
  data={[
    {
      name: 'checked',
      description: '受控模式下的开关状态',
      type: 'boolean'
    },
    {
      name: 'defaultChecked',
      description: '非受控模式下的默认开关状态',
      type: 'boolean'
    },
    {
      name: 'onCheckedChange',
      description: '开关状态变化时触发',
      type: '(checked: boolean) => void'
    },
    {
      name: 'color',
      description: '打开状态的主题颜色',
      type: "'primary' | 'destructive' | 'success' | 'warning' | 'info' | 'carbon' | 'secondary' | 'accent'",
      default: "'primary'"
    },
    {
      name: 'size',
      description: '尺寸，影响轨道和滑块大小',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'children',
      description: '滑块内部内容，会渲染到 SwitchThumb 内',
      type: 'ReactNode'
    },
    {
      name: 'disabled',
      description: '是否禁用开关',
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'required',
      description: '是否必填（表单场景）',
      type: 'boolean'
    },
    {
      name: 'name',
      description: '表单字段名称',
      type: 'string'
    },
    {
      name: 'value',
      description: '表单提交时的值',
      type: 'string',
      default: "'on'"
    },
    {
      name: 'className',
      description: '轨道的自定义 class；传入有效值时优先于 classNames.root',
      type: 'ClassValue'
    },
    {
      name: 'classNames',
      description: '各 slot 的自定义 class（root / thumb）',
      type: 'ClassNames'
    },
    {
      name: 'asChild',
      description: '将渲染委托给子元素',
      type: 'boolean',
      default: 'false'
    }
  ]}
/>

### SwitchRoot

底层轨道组件，基于 Radix Switch Root 封装。适合需要自行组合 thumb 时使用。

<PropsTable
  data={[
    {
      name: 'color',
      description: '打开状态的主题颜色',
      type: "'primary' | 'destructive' | 'success' | 'warning' | 'info' | 'carbon' | 'secondary' | 'accent'",
      default: "'primary'"
    },
    {
      name: 'size',
      description: '尺寸，影响轨道大小',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'checked',
      description: '受控模式下的开关状态',
      type: 'boolean'
    },
    {
      name: 'defaultChecked',
      description: '非受控模式下的默认开关状态',
      type: 'boolean'
    },
    {
      name: 'onCheckedChange',
      description: '开关状态变化时触发',
      type: '(checked: boolean) => void'
    },
    {
      name: 'disabled',
      description: '是否禁用开关',
      type: 'boolean',
      default: 'false'
    },
    {
      name: 'className',
      description: '轨道自定义 class',
      type: 'ClassValue'
    }
  ]}
/>

### SwitchThumb

底层滑块组件，基于 Radix Switch Thumb 封装。主组件会自动创建 thumb；只有自定义结构时才需要直接使用。

<PropsTable
  data={[
    {
      name: 'size',
      description: '尺寸，影响滑块大小和位移距离',
      type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'",
      default: "'md'"
    },
    {
      name: 'children',
      description: '滑块内部内容，例如图标',
      type: 'ReactNode'
    },
    {
      name: 'className',
      description: '滑块自定义 class',
      type: 'ClassValue'
    }
  ]}
/>

## 类型

<TypeTable
  data={[
    {
      name: 'SwitchProps',
      description: '主组件属性。继承 SwitchRootProps，并增加 classNames 配置；children 会渲染到 SwitchThumb 内。',
      fields: [
        { name: 'checked', type: 'boolean', description: '受控开关状态。' },
        { name: 'className', type: 'ClassValue', description: '轨道 class。' },
        { name: 'classNames', type: 'ClassNames', description: '各 slot 的自定义 class。' },
        { name: 'color', type: "'primary' | 'destructive' | 'success' | 'warning' | 'info' | 'carbon' | 'secondary' | 'accent'", description: '主题颜色。' },
        { name: 'children', type: 'ReactNode', description: '滑块内部内容。' },
        { name: 'defaultChecked', type: 'boolean', description: '非受控默认状态。' },
        { name: 'disabled', type: 'boolean', description: '是否禁用。' },
        { name: 'name', type: 'string', description: '表单字段名称。' },
        { name: 'onCheckedChange', type: '(checked: boolean) => void', description: '状态变化回调。' },
        { name: 'required', type: 'boolean', description: '是否必填。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '组件尺寸。' },
        { name: 'value', type: 'string', description: '表单提交时的值。' }
      ]
    },
    {
      name: 'SwitchRootProps',
      description: '轨道组件属性。继承 Radix Switch Root 的常用状态、表单和无障碍属性，并增加主题颜色和尺寸。',
      fields: [
        { name: 'checked', type: 'boolean', description: '受控开关状态。' },
        { name: 'defaultChecked', type: 'boolean', description: '非受控默认状态。' },
        { name: 'onCheckedChange', type: '(checked: boolean) => void', description: '状态变化回调。' },
        { name: 'color', type: "'primary' | 'destructive' | 'success' | 'warning' | 'info' | 'carbon' | 'secondary' | 'accent'", description: '主题颜色。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '轨道尺寸。' },
        { name: 'disabled', type: 'boolean', description: '是否禁用。' },
        { name: 'required', type: 'boolean', description: '是否必填。' },
        { name: 'name', type: 'string', description: '表单字段名称。' },
        { name: 'value', type: 'string', description: '表单提交时的值。' },
        { name: 'className', type: 'ClassValue', description: '轨道 class。' },
        { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。' },
        { name: '...buttonProps', type: 'button element props', description: '其他 Radix Switch Root / button 属性。' }
      ]
    },
    {
      name: 'SwitchThumbProps',
      description: '滑块组件属性。继承 Radix Switch Thumb 属性，并增加尺寸和自定义 class。',
      fields: [
        { name: 'children', type: 'ReactNode', description: '滑块内部内容。' },
        { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl'", description: '滑块尺寸和选中态位移。' },
        { name: 'className', type: 'ClassValue', description: '滑块 class。' },
        { name: 'asChild', type: 'boolean', description: '将渲染委托给子元素。' }
      ]
    },
    {
      name: 'ClassNames',
      description: 'Switch 各 slot 的自定义 class。',
      fields: [
        { name: 'root', type: 'ClassValue', description: '轨道容器的 class。' },
        { name: 'thumb', type: 'ClassValue', description: '滑块的 class。' }
      ]
    }
  ]}
/>

<UnionType
  name="ClassValue"
  description="CSS 类名类型"
  type="string | null | undefined | Record<string, boolean> | ClassValue[]"
/>
